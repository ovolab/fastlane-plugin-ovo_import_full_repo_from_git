# fastlane-plugin-ovo_import_full_repo_from_git
This Fastlane plugin import Fastfile from git with full checkout and version resolution, a drop-in alternative to import_from_git that checks out the full repository before importing the Fastfile
